我将检查并修复所有在深色模式下可能导致文字或图标不可见（硬编码为黑色/深色）的地方。主要目标是将硬编码的 `BrandPrimary`（浓墨色，0xFF1A1A1A）替换为主题感知的 `MaterialTheme.colorScheme.primary` 或 `onSurface`。

具体步骤如下：

### 1. 修复设置对话框 (SettingsDialog.kt)
- 在 `SettingsSection` 组件中，将标题文字和图标的颜色从 `BrandPrimary` 改为 `MaterialTheme.colorScheme.primary`。
- 将“录入新灵犀”按钮的图标颜色改为主题感知颜色。
- 确保所有 `OutlinedTextField` 的标签和文字颜色符合主题。

### 2. 优化任务流容器 (TaskFlowContainer.kt)
- 将“推演记录”的标题和图标颜色从硬编码的 `BrandPrimary` 改为 `MaterialTheme.colorScheme.onSurfaceVariant`，确保在深色模式下清晰可见。

### 3. 调整消息气泡装饰 (MessageBubbles.kt)
- 将 AI 消息气泡左侧的装饰“墨线”渐变色从硬编码的 `BrandPrimary` 改为主题感知颜色，使其在深色背景下也能呈现出淡淡的质感。

### 4. 规范启动页 (SplashScreen.kt)
- 虽然目前已有简单的深色判断，但建议将其改为使用主题色引用，以保持全局一致性。

### 5. 代码清理
- 移除 `SessionItem.kt` 等文件中不再需要的 `BrandPrimary` 引用。

完成后，深色模式下的所有文字和关键图标都将自动适配主题，不会再出现“黑底黑字”的问题。